# Validator Configuration
# ======================

# Number of fuzz iterations per generated equivalence test
fuzz_runs=2000

# Hevm Configuration
# ------------------
# Enable/disable hevm symbolic verification (true/false).
# Off by default: hevm is an optional extra tool. When enabled but not
# installed the check is reported as skipped, never as a failure.
hevm_enabled=false

# Hevm timeout in seconds (overall process timeout)
hevm_timeout=300

# Hevm solver timeout in milliseconds (Z3 SMT solver per-query timeout)
hevm_solver_timeout=30000

# Max loop iterations for hevm (prevents infinite loop explosion)
hevm_max_iterations=5

# Fuzz Test Configuration
# -----------------------
# Max array length for fuzz testing
max_array_length=5

# Gas Gate
# --------
# Require the candidate to be strictly cheaper on at least one function.
# When false, gas is measured and reported but never blocks acceptance.
require_gas_improvement=true

# Per-command timeout for `forge` invocations, in seconds
forge_timeout=900
