[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"

# Solidity compiler version
solc_version = "0.8.20"

# Fuzz testing settings
[fuzz]
runs = 1000
max_test_rejects = 65536

# Invariant testing settings  
[invariant]
runs = 256
depth = 15
